# CLAUDE.md

## `doc/design/` の Design Doc は現在の仕様の情報源にしない

Design Doc の有効期間は作成された作業ブランチが生きている間のみ。マージ・削除後は「作成時点の設計意図を記録した歴史的資料」であり、以後更新されない前提で扱う。現在の仕様・挙動を知りたいときはソースコード・テスト・コミットログを一次情報にすること。

- 例: 現在の仕様を把握するためのコード探索では `doc/design/` 配下は検索対象からまるごと除外する。

新しい Design Doc を作成する場合は `design-doc` skill を使う。
